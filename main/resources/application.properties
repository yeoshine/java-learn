spring.application.name=demo

# MySQL 数据库配置。
# 可通过环境变量 DB_HOST、DB_PORT、DB_NAME、DB_USERNAME、DB_PASSWORD 覆盖默认值。
spring.datasource.url=jdbc:mysql://${DB_HOST:8.136.35.70}:${DB_PORT:3306}/${DB_NAME:demo}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${DB_USERNAME:demo}
spring.datasource.password=${DB_PASSWORD:demo111243}

# JPA 配置。
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT 配置。
jwt.secret=demo-jwt-secret-key-please-change-in-production-2026-demo
jwt.expiration=86400000
